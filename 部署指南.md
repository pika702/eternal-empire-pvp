# 🚀 永恒帝国 PvP 服务器 - 部署指南

## ✅ 已完成

- [x] 服务器代码已准备好 (`server/pvp_server.js`)
- [x] Git 仓库已初始化并包含所有必要文件
- [x] 测试通过（PvP 功能正常）
- [x] Render.com 配置文件已准备 (`render.yaml`, `.renderignore`)

## ⚠️ 需要你授权的操作（3步）

### 步骤 1：创建 GitHub 仓库

1. 打开浏览器访问：**https://github.com/new**
2. 创建新仓库：
   - **Repository name**: `eternal-empire-pvp`
   - **Description**: 永恒帝国：龙之纪元 - 实时对战 WebSocket 服务器
   - ✅ 勾选 "Public"（公开仓库）
   - ❌ **不要勾选** "Initialize this repository with a README"
   - 点击 **"Create repository"**

---

### 步骤 2：推送代码到 GitHub

在你的电脑上打开 **PowerShell** 或 **CMD**，执行以下命令：

```powershell
cd "D:\Users\Administrator\Desktop\永恒帝国-龙之纪元"

# 配置 Git 用户信息（如果还没配置）
git config --global user.email "developer@eternal-empire.com"
git config --global user.name "Eternal Empire Developer"

# 添加远程仓库（替换 YOUR_USERNAME 为你的 GitHub 用户名）
git remote remove origin 2>$null
git remote add origin https://github.com/YOUR_USERNAME/eternal-empire-pvp.git

# 设置分支名称
git branch -M main

# 推送代码（会要求输入用户名和密码/Token）
git push -u origin main
```

**注意**：首次推送时需要 GitHub 用户名和 **Personal Access Token**（不是 GitHub 密码）。

**如何获取 Personal Access Token**：
1. 访问 https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 勾选 `repo` 权限
4. 点击 "Generate token"
5. 复制生成的 Token（只显示一次！）

---

### 步骤 3：部署到 Render.com

1. 访问：**https://dashboard.render.com/register**
2. 点击 **"Sign up with GitHub"**
3. 授权 Render 访问你的 GitHub 账号
4. 登录后点击蓝色按钮 **"New +"** → **"Web Service"**
5. 选择 `eternal-empire-pvp` 仓库
6. 配置参数：
   | 设置项 | 值 |
   |--------|-----|
   | Name | `eternal-empire-pvp` |
   | Root Directory | `server` |
   | Branch | `main` |
   | Build Command | `npm install` |
   | Start Command | `node pvp_server.js` |
   | Instance Type | **Free** |
7. 点击 **"Create Web Service"**

---

## 📋 部署完成后

1. 在 Render 控制台查看部署状态，等待绿色进度条
2. 复制你的公网 URL（格式如：`https://eternal-empire-pvp.onrender.com`）
3. 告诉我你的域名，我会帮你更新游戏代码中的服务器地址

---

## 🔍 验证部署成功

部署完成后，访问：
```
https://你的域名.onrender.com/health
```

应该返回：
```json
{"status":"ok","uptime":123,"rooms":0,"players":0}
```

---

## 📝 文件清单

```
永恒帝国-龙之纪元/
├── server/
│   ├── pvp_server.js     # 主服务器（9.7KB，359行）
│   ├── package.json      # 依赖配置
│   ├── .renderignore     # Render 忽略规则
│   ├── render.yaml       # Render 配置
│   ├── test.html         # 测试页面
│   └── 一键部署.bat      # Windows 自动部署脚本
├── game/
│   └── 永恒帝国-龙之纪元.html  # 游戏本体（已集成PvP按钮）
└── README.md             # 项目说明
```

---

## ⚠️ 重要提示

| 问题 | 解决方案 |
|------|----------|
| Glitch 已关闭 | 使用 Render.com 替代 |
| 免费套餐会休眠 | 使用 UptimeRobot 每5分钟 ping 一次 |
| 连接失败 | 确保使用 `wss://` 而非 `ws://` |

---

**更新时间**: 2026-09-06  
**状态**: 等待 GitHub 授权后部署
