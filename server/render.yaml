services:
  - type: web
    name: eternal-empire-pvp
    env: node
    region: oregon
    plan: free
    buildCommand: npm install
    startCommand: node pvp_server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
